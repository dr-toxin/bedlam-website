# The Bedlam Brothers website (demo)

Static site, no build step. `index.html` is the whole page. Images live in `assets/`.

Served with GitHub Pages at demo.bedlambrothers.band (see `CNAME`).

The page carries `noindex, nofollow` while it is a demo. Remove that meta tag at launch.
